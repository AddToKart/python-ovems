Flask==2.3.3
mysql-connector-python==8.1.0

# Additional recommended packages for production use (optional)
# Werkzeug==2.3.7  # WSGI utilities (comes with Flask)
# Jinja2==3.1.2     # Template engine (comes with Flask)
# MarkupSafe==2.1.3 # Safe string handling (comes with Flask)
